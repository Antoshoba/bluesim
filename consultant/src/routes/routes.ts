const ROOT = "/";

// Auth Routes
const LOGIN = "/login";
const LOGOUT = "/logout";

const DASHBOARD = "/dashboard";

const routes = {
  ROOT,
  LOGIN,
  LOGOUT,
  DASHBOARD,
};

export default routes;
