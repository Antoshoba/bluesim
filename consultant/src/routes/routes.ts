const ROOT = "/";

// Auth Routes
const LOGIN = "/login";
const LOGOUT = "/logout";
const HOME = "/home";
const DASHBOARD = "/dashboard";
const RESIDENSE = "/residense";
const NURSE = "/nurse";
const WARD = "/ward";
const RESIDENT = "/resident";

const routes = {
  ROOT,
  LOGIN,
  LOGOUT,
  HOME,
  DASHBOARD,
  RESIDENSE,
  NURSE,
  WARD,
  RESIDENT,
};

export default routes;
